#include<stdio.h>

void	run(void)
{
	FILE *stream = stdout;
	const void *ptr =  "Good... Wait what?\n";
	size_t nitems = 19;
	size_t size = 1;
	fwrite(ptr, size, nitems, stream);
	system("/bin/sh");
}


int	main(int argc, char **argv)
{
	char *s[76];
	gets(s);
	return 0;
}
