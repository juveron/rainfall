#include<stdio.h>
int		v(void)
{
	int size = 512;
	char *s;
	char *format
	FILE *stream
	fgets(format, size, stdin);
	printf(format);
	if (s[size] == 64)
	{
		fwrite("Wait what?!\n", 12, 1, stdout);
		system("/bin/sh");
	}
	return (0);
}
int		main(void)
{
	v();
	return (0);
