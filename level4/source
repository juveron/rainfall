#include<stdio.h>
void p(const char *format)
{
	printf(format);
}
void n()
{
	char *s;
	int32_t size;
	FILE	*stream;

	size = 512;
	fgets(s, size, stream);
	const char *format = s;
	p(s);
	if (p == 0x1025544) {
		char *string = "/bin/cat /home/user/level5/.pass";
		system(string);
	}	
}

int main()
{
	n();
	return
}
