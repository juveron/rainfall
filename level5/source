#include<stdio.h>

void	o();
{
	const char *string = "/bin/sh";
	system(string);
	int status = 1;
	exit(status);
}

void	n();
{
	char *format
	int32_t size = 512;
	FILE stream;
	char *s;
	fgets(s, size, stream);
	const char *format = format;
	printf(format);
	int status = 1;
	exit(status);
}

int	main() {
	n();
	return 0;
}
