#include<stdio.h>

void	m();
{
	time_t	*timer = 0;
	time(timer);
	const char *format = "%s - %d\n";
	printf(format);
}

char *c = "0";

int		main(int argc, char *argv)
{
	char *mem1;
	char *mem2;
	const char *mode
	const char *filename = argv[1];
	FILE *stream
	int size = 68;


	mem1 = malloc(8);
	mem1[0] = 1;
	mem1[1] = malloc(8);
	mem2 = malloc(8);
	mem2[0] = 1;
	mem2[1] = malloc(8);

	strcpy(mem1[1], argv[1]);
	strcpy(mem2[1], argv[2]);
	fopen(filename, mem2[2]);
	fgets("/home/user/level8/.pass", size, stream);
	const char *s = "~~";
	puts(s);
}
